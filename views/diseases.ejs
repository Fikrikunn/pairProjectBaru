<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hasil Diagnosa</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container" style="margin-top: 100px;">
        <a href="/profile" class="btn btn-warning mb-4">← Back to Profile</a>
        <h1 style="margin-top: 50px;" class="text-success">Hasil Diagnosa Penyakit Anda:</h1>

        <% if (diseases.length > 0) { %>
            <ul class="list-group">
                <% diseases.forEach(disease => { %>
                    <li class="list-group-item">
                        <strong><%= disease.name %></strong> - <%= disease.description %>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <p class="text-danger">Tidak ada penyakit yang cocok dengan gejala yang Anda pilih.</p>
        <% } %>

        <h1 style="color: orange; margin-top: 50px;">Terimakasih, silahkan Cek Email anda untuk info lebih lanjut.. 😇🙏🏼</h1>
    </div>
</body>
</html>
